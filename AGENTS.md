# WeiqiXN Agent 规范

## 项目入口

- `UnityProject/` 是 Unity 项目。项目文档入口见 `UnityProject/doc-governance/UnityProject/AGENTS.md`。
- `ConfigExporter/` 是导表工具。工具约束见 `ConfigExporter/AGENTS.md`。

## 文档读取策略

- 先根据本文件判断任务归属，再读取对应子项目入口文档。
- `UnityProject` 任务默认读取 `UnityProject/doc-governance/UnityProject/AGENTS.md`，再按改动范围补读核心文档或模块文档。
- `ConfigExporter` 任务默认读取 `ConfigExporter/AGENTS.md` 和相关源码，除非变更会影响 Unity 数据消费侧。
- 只在任务涉及当前行为、架构边界、阶段计划或文档同步时读取 `SPECIFICATION.md`、`ARCHITECTURE.md`、`ROADMAP.md`。
- `modules/` 下文档按任务涉及模块定向读取，不默认全量读取。

## 通用编码规范

- 代码结构必须简洁明确，优先使用当前项目已有的分层、命名和调用方式。
- 不做过度设计，不为了未来可能出现的需求提前增加抽象层、适配层或包装层。
- 不做过度封装；只有当封装能减少真实复杂度、消除重复、稳定边界或提升可读性时才新增公共方法、类或模块。
- 同一逻辑出现超过 3 行重复代码时，必须进行必要的封装复用；封装后仍应保持调用关系直观。
- 修改代码时优先收敛现有实现，不引入与当前职责无关的框架、模式或通用工具。
- 修改必须聚焦当前任务范围，不做无关重构、无关格式化或无关文件整理。
- 发现相邻问题时，除非会阻塞当前任务，否则只在交付说明中记录，不顺手扩大修改范围。
- 修改已有逻辑前必须先理解当前调用链和数据流，不用“看起来更合理”的新结构替换已有稳定路径。
- 不为小范围需求引入新依赖、新框架或新全局工具；确实需要时必须说明现有能力为什么不足。
- 优先使用项目已有工具、已有模块和语言标准库。
- 命名应表达业务职责，不使用 `Manager`、`Helper`、`Util` 等泛化名称，除非项目已有明确同类模式。
- 类、方法和文件只承担一个清晰职责；当名称需要用“和/以及/并且”解释时，应重新检查职责边界。
- 公共接口要比内部实现更稳定，不把临时实现细节暴露成公共 API。
- 不吞异常；捕获异常时必须保留可诊断信息，并让调用方能知道失败状态。
- 日志只记录对定位问题有价值的信息，不输出大段临时调试内容。
- 注释用于解释约束、原因和非显然决策，不复述代码字面行为。
- 不手改生成物，除非该文件明确是人工维护文件。

## 文档同步规范

- 只有当前行为、架构边界、阶段计划或入口约束发生变化时才更新权威文档。
- 不为普通实现细节、临时调试过程或未落地设想更新长期文档。
- 文档更新应写稳定事实和约束，不记录一次性过程流水。
- 用户报告或执行过程中确认的未修复 bug，必须先记录到对应子项目约定的已知问题文档，再开始逐个修复，方便后续会话重新定位；修复并完成最小验证后，再按该文档规则移除或更新对应条目。

## 完成要求

- 每次实现后检查是否触发 `UnityProject` 或 `ConfigExporter` 的子项目文档更新。
- 新增或修改代码后按对应子项目文档执行必要检查；无法执行时在交付说明中记录原因。
- 修改后必须执行与改动范围匹配的最小验证；不能执行时需要说明原因和剩余风险。
- 修复 bug 时，优先补充能覆盖该问题的测试、示例或可重复验证步骤。
