// This file is auto generated by config exporter
using Newtonsoft.Json.Linq;
using System;
using System.Collections.Generic;
using System.IO;
using XNClient.Logger;

public class ${class_name}
{
${fields}

    public static Dictionary<string, ${class_name}> ${dict_class_name};

    public static ${class_name} GetConfigData(string id)
    {
        if (${dict_class_name} == null) {
            ${dict_class_name} = new Dictionary<string, ${class_name}>();
            var jsonObj = GlobalConfig.GetJsonConfigJObject("${json_file_name}");
            if (jsonObj != null) {
                foreach (var property in jsonObj.Properties()) {
                    try {
                        var item = property.Value.ToObject<${class_name}>();
                        ${dict_class_name}[property.Name] = item;
                    }
                    catch (Exception ex) {
                        XNLogger.LogError($"读表错误，跳过条目 {property.Name}: {ex.Message}");
                    }
                }
            }
        }
        if (${dict_class_name}.TryGetValue(id, out ${class_name} data)) {
            return data;
        } else {
            return null;
        }
    }
}
