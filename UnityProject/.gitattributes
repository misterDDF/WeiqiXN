# Auto detect text files and perform LF normalization
* text=auto

# KataGo local engine and model artifacts are intentionally committed so a
# fresh clone can run the editor-side AI smoke test without extra downloads.
ExternalTools/KataGo/**/*.exe binary
ExternalTools/KataGo/**/*.dll binary
ExternalTools/KataGo/**/*.bin.gz binary
